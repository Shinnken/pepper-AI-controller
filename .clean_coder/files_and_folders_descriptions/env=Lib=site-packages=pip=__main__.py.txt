File responsible for handling the entry point and initialization of the pip package when run as a script.

The code ensures proper sys.path configuration, handles running from a wheel, and sets up the main entry point for pip's command-line interface. It removes current directory from sys.path to avoid potential conflicts and adds the wheel path if running from a wheel. The script suppresses specific deprecation warnings and calls the main pip CLI function.